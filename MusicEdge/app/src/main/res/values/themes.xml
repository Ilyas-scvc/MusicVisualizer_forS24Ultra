<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The XML theme only has to carry the app through to the first Compose frame:
        DeviceDefault picks up the device system font and One UI look on Samsung
        devices. Edge-to-edge and system bar colors are handled in MainActivity.
    -->
    <style name="Theme.MusicEdge" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
